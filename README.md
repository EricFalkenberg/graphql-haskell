# GraphQL Library for Haskell

This project is built with [The Haskell Tool Stack](https://docs.haskellstack.org/en/stable/README/).

## Build
```sh
stack build --ghc-options -threaded
stack exec graphql-haskell-exec
```
